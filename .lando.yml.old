name: lando-laravel
recipe: laravel
config:
  webroot: app/public
  composer_version: 2.7.5
  via: 'apache:2.4'
  cache: none
  xdebug: false
services:
  appserver:
    type: php:8.1
  database:
    type: mysql:5.7
